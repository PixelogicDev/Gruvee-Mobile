import React, { useRef, useState } from 'react'
import {
    Alert,
    Animated,
    Dimensions,
    Text,
    View,
    FlatList,
    ImageBackground,
    TouchableOpacity,
    StyleSheet,
} from 'react-native'

const SongItemCommentBar = () => {
    return <Text>I AM THE COMMENT BAR</Text>
}

export default SongItemCommentBar
