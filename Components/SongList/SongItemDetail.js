import React, { useRef, useState } from 'react'
import {
    Alert,
    Animated,
    Dimensions,
    Text,
    View,
    FlatList,
    Fragment,
    ImageBackground,
    TouchableOpacity,
    StyleSheet,
} from 'react-native'

const SongItemDetail = () => {
    return <Text>I AM A SONG ITEM DETAIL </Text>
}

export default SongItemDetail
